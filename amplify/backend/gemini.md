# Amplify Backend Context

This directory contains the configuration for the AWS Amplify backend. **Warning:** Do not modify files here directly unless you are using the Amplify CLI. Manual changes can be overwritten.

## Backend Services

- **API:**
    - **Type:** GraphQL
    - **Name:** `shutokunre`
    - **Schema:** The GraphQL schema is defined in `amplify/backend/api/shutokunre/schema.graphql`.
    - **Generated Code:** The TypeScript types and client code for the API are generated by Amplify and located in the root `src/` directory (`API.ts`, `graphql/`).

- **Authentication:**
    - **Type:** Amazon Cognito
    - **Name:** `shutokunre54657861`
    - **Configuration:** Managed via `amplify/backend/auth/shutokunre54657861/cli-inputs.json`.

## Deployment

- All backend resources are defined as Infrastructure as Code (IaC) using AWS CloudFormation.
- To deploy changes to the backend, use the Amplify CLI command `amplify push`.
- To pull the latest backend definition, use `amplify pull`.
